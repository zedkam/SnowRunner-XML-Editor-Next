# SnowRunner XML Editor Next — состояние проекта

Дата диагностики: 2026-09-05

Этот файл описывает состояние локальной копии и проверенной установки игры. Репозиторий подключен к `zedkam/SnowRunner-XML-Editor-Next`, upstream оригинального проекта сохранён отдельно; актуальный `initial.pak` найден на диске D и проверен в режиме чтения.

## Краткий вывод

Локальная копия подготовлена как тестовая версия продолжения проекта:
`2.0.0-beta.4`. Публичный репозиторий: `zedkam/SnowRunner-XML-Editor-Next`.

Проект представляет собой рабочий Electron/Vue/TypeScript-редактор XML с хорошей базовой моделью разрешения файлов: модификация имеет приоритет над DLC, DLC — над базовой игрой. Главный технический долг связан не с самим XML-доменом, а с тем, что состав игрового контента зафиксирован в артефактах сборки и в сгенерированном статическом каталоге.

Для продолжения развития используется индекс фактического содержимого установленной игры. При этом текущий редактор, bridge и UI мигрируются постепенно.

## Доказательства

| Область | Наблюдение | Последствие |
|---|---|---|
| Репозиторий | Рабочая копия подключена к `zedkam/SnowRunner-XML-Editor-Next`; исходный remote сохранён как upstream | История продолжения и источник оригинала разделены |
| Версия | `package.json` содержит `2.0.0-beta.4` | Версия приложения не является версией игрового контента; snapshot архива фиксируется отдельно |
| Зависимости | `node_modules` и `package-lock.json` восстановлены; лишний `@types/eslint__js` удалён | Установка воспроизводима; `npm run check` проходит |
| Тесты | Файлы `*.test.*`, `*.spec.*` и `tests/` отсутствовали | Нет регрессии для XML, DLC, архива и сериализации |
| Архив | `D:\SteamLibrary\steamapps\common\SnowRunner\preload\paks\client\initial.pak`: 29 073 408 байт, 11 719 записей; найден `dlc_18_1` | Состав актуальной установки подтверждён; 495 XML проверены, неожиданных ошибок нет |
| Extraction | В optimized list добавлены `dlc_15_5`, `dlc_16`, `dlc_16_5`, `dlc_17`, `dlc_17_5`, `dlc_18`, `dlc_18_1` | Новые XML будут извлекаться при оптимизированном режиме |
| Каталог defaults | Обновлён `src/modules/data/defaults/generated.ts`: 494 ключа из 495 XML новых DLC | Reset для новых источников получил generated baseline; один collision задокументирован |
| Изображения | Для 15 машин извлечены изображения из PCT-страниц текущего `gfx.pak`; маппинг сохранён в manifest | `generated-image-links.json` хранит `sourceFile`, `texturePage`, `mappingMethod`; `copied=15`, `missing=0` |
| Описания | `strings_russian.str` и `strings_english.str` загружаются одновременно | В карточках техники доступны проверенные русская и английская версии описания |
| README | Публичная страница переведена на английский и русский языки | Служебный раздел для разработчиков удалён из GitHub README |
| DLC discovery | `src/modules/dlcs/main.ts` сканирует уже распакованную папку `_dlc` | Обнаружение работает после извлечения, но не решает выбор полного/оптимизированного списка |
| XML resolution | `src/modules/xml/game/game-xml.ts` ищет `mod → DLC → base` | Базовый приоритет хороший и должен стать частью отдельного Content Provider |
| Проверка | `npm run diagnose`, `npm run check`, `npm run lint` и `test:roundtrip` проходят; lint сохраняет исторические warnings; `npm run package` успешно собирает Inno Setup installer | Готовый EXE создан в `out/SnowRunnerXMLEditor.exe`; контент новых DLC встроен в сборку |

## Состояние поддержки новых DLC

Официально уже выпущены Season 17: Repair & Rescue и Season 18: Patch & Power. В текущей установленной версии дополнительно найден source `dlc_18_1` с Mercedes-Benz 3850 и Mercedes Mamute 1519.

Состав XML в актуальном архиве подтверждён: `dlc_15_5`, `dlc_16`, `dlc_16_5`, `dlc_17`, `dlc_17_5`, `dlc_18`, `dlc_18_1`. Extraction и generated defaults обновлены. Из 495 XML-файлов 479 проходят строгий XML-парсер; 16 отмечены как известные duplicate-attribute quirks retail-архива, неожиданных ошибок — 0. Round-trip для обеих новых машин пройден; полный UI smoke-тест остаётся отдельной проверкой.

## Приоритетные риски

1. Будущие пути `_dlc` всё ещё не добавляются автоматически в статический optimized list.
2. Defaults и reset опираются на огромный вручную/генераторно собранный TypeScript-файл вместо данных конкретной установки.
3. XML пересериализуется через Cheerio и форматтер; без snapshot-тестов легко потерять порядок, неизвестные атрибуты или особые узлы.
4. Архивирование через WinRAR зависит от встроенного бинарного набора и статических list-файлов.
5. Bridge между main и renderer типизирован частично и широко использует `any`; ошибка контракта проявится только во время выполнения.
6. Нет отдельного понятия «версия игрового контента», поэтому результаты для разных патчей и DLC нельзя надёжно сравнивать.

## Принятые архитектурные решения

- Источником истины для доступности объекта является фактический XML/архив установленной игры, а не список DLC в исходниках.
- Номер DLC используется как метка происхождения, но не как ветка бизнес-логики.
- Неизвестные XML-узлы и атрибуты должны сохраняться; UI может их показывать как raw/advanced, но не должен молча удалять.
- `defaults` остаётся временным fallback до миграции, после чего его роль смещается к snapshot-базе и миграциям.
- Сохранение делается транзакционно: backup → temporary archive update → verify → replace.

Подробная целевая схема находится в [`docs/architecture/README.md`](docs/architecture/README.md), проверенный inventory — в [`docs/architecture/VERIFIED_ARCHIVE_INVENTORY.md`](docs/architecture/VERIFIED_ARCHIVE_INVENTORY.md), результат генерации — в [`docs/architecture/GENERATED_CONTENT.md`](docs/architecture/GENERATED_CONTENT.md), а план внедрения — в [`docs/architecture/ROADMAP.md`](docs/architecture/ROADMAP.md).
