# Season 18 fixture placeholder

Добавить после получения легально доступного XML-фрагмента из собственной установки:

- truck `Mercer R230`;
- truck `HIB Billert M816`;
- `Gooseneck Semi-Trailer with Parts` или его фактический XML id;
- связанные referenced XML и `_templates`;
- source id из фактического пути `_dlc`.
